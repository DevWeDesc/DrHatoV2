events {
    worker_connections 1024;
}

http {
    server {
    listen 80;
    server_name devdilan.online;

    set_real_ip_from 10.0.0.0/8;
    set_real_ip_from 172.16.0.0/12;
    set_real_ip_from 192.168.0.0/16;
    set_real_ip_from 0.0.0.0/0;


        location / {
        proxy_pass http://host.docker.internal:3333;
        proxy_http_version 1.1;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Server $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $http_host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
    }

    # location  / {
    #     return 301 https://$host$request_uri;
    # }

    # location ~ /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    # }
}
}


# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name devdilan.online;

#     ssl_certificate /etc/letsencrypt/live/devdilan.online/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/devdilan.online/privkey.pem;
    

#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

#     location / {
#         proxy_pass http://host.docker.internal:3000;
#         proxy_http_version 1.1;
#         proxy_set_header X-Forwarded-Host $host;
#         proxy_set_header X-Forwarded-Server $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Host $http_host;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "Upgrade";
#     }

#     location ~ /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }

#     real_ip_header X-Forwarded-For;
# }

